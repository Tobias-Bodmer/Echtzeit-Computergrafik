import { Color, Scene } from 'https://cdnjs.cloudflare.com/ajax/libs/three.js/r126/three.module.js';

function createScene() {
    const scene = new Scene();

    return scene;
}

export { createScene };